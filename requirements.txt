# Pip layer for the Hist2Pheno conda env (see environment.yml).
# Versions match the working SeededNTM environment (Python 3.11).
# Keep NumPy 1.x: NumPy 2.x breaks some pandas / scanpy combinations used here.
# PyTorch / torchvision: install a CUDA wheel after `conda env create` (README).

# Core ML / spatial
numpy>=1.26,<2
pandas>=2.2
scikit-learn>=1.5
scipy>=1.13
scanpy>=1.11
anndata>=0.11
h5py>=3.11
zarr>=2.18

# Visualization / notebooks
matplotlib>=3.9
seaborn>=0.13
jupyterlab>=4.0
ipykernel>=6.29
tqdm>=4.66
openpyxl>=3.1

# Image / embeddings
Pillow>=10.0
scikit-image>=0.24
tifffile>=2024.1
timm>=1.0
einops>=0.8
opencv-python-headless>=4.8

# Xenium / spatial (optional for CODEX-only)
spatialdata>=0.5
geopandas>=1.0
squidpy>=1.6

# UNI / HIPT / Virchow2 weights are not bundled
